import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<something-list></something-list>'       // put the component direct; later it can be replaced by router to add other features to application.
})
export class AppComponent {
}
